<div class="relative">
    <input type={{ $type }} placeholder="{{ $placeholder }}"
      name="{{ $name }}" id="{{ $name }}" value="{{ $value }}"
      class="w-full rounded-md border-0 py-1.5 px-2.5 pr-8 text-sm ring-2 ring-slate-300 placeholder:text-slate-400 focus:ring-2 focus:ring-offset-2 focus:ring-offset-slate-50"
    />
  </div>
</div>